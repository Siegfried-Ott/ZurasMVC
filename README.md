# ZurasMVC
# cd public; php -S localhost:5000

# cd /f/inetpub/wwwroot/ZurasMVC
# git log
# git add .
# git commit -m "initial commit"
# git push master origin
# 
# git pull origin master
# 
# https://github.com/Siegfried-Ott
# cd /f/inetpub/wwwroot/ZurasMVC
# git-push - Update remote refs along with associated objects
# git-push - &lt;branch&gt; &lt;repository&gt;Update remote 
# refs to repository

# git-pull - &lt;branch&gt; &lt;repository&gt;Fetch from and integrate with 
#            another repository or a local branch

# git init --help - Get help to a command

# git init    - Create an empty Git repository or 
#               reinitialize an existing one
# git init -b "branch-name"
# nano ~/.gitconfig
# git config --global user.name  "Siegfried Ott"
# git config --global user.email "emilmeier@hotmail.com"
# git config --global init.defaultBranch "master"

# git clone git@github.com:Siegfried-Ott/ZurasMVC.git
#          - Clone a repository into a new directory

# git fetch origin -&gt; Download objects and refs from another repository

# git-add    - Add file contents to the index
# git add .  - Adds all files 
# git add -A - Adds all files recursively

# git-log    -  Show commit logs 
# git-commit -  Record changes to the repository
#            -  Displays text editor to enter the commit message
#               leave the editor with saving the document
# git-commit -m "&lt;msg&gt;"- Record changes to the repository 
#               and &lt;msg&gt; as commit message.

# git remote ...
#            Manage the set of repositories ("remotes") whose branches 
#            you track.
# git remote add origin git@github.com:Siegfried-Ott/ZurasMVC.git
#            Add a remote named &lt;name&gt; for the repository 
#            at &lt;URL&gt;.
# git-merge    - Join two or more development histories together
# git-show     - Show various types of objects
# git-show-ref - List references in a local repository

# …or create a new repository on the command line
# echo "# ZurasMVC" >> README.md
# git init
# git add README.md
# git commit -m "first commit"
# git branch -M master
# git remote add origin git@github.com:Siegfried-Ott/ZurasMVC.git
# git push -u origin master


My owen php framework
ZurasMVC
